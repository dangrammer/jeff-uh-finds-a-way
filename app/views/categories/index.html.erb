<br>
### temporary identification heading: categories/index ###
<% @categories.each do |category| %>
  <h3><%= link_to category.name, category_path(category) %></h3>
  <p><%= category.description %></p>
  <br>
<% end %>


